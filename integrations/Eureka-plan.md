     ╭───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
     │ План реализации интеграции сервиса Eureka (обновлённый)                                                                                                                           │
     │                                                                                                                                                                                   │
     │ Фаза 1: Модели БД и базовая инфраструктура                                                                                                                                        │
     │                                                                                                                                                                                   │
     │ 1. Создание моделей для Eureka (app/models/eureka.py):                                                                                                                            │
     │   - EurekaServer - серверы реестра Eureka                                                                                                                                         │
     │   - EurekaApplication - приложения в Eureka                                                                                                                                       │
     │   - EurekaInstance - экземпляры сервисов (instance_id формата IP:service-name:port)                                                                                               │
     │   - EurekaInstanceStatusHistory - история статусов                                                                                                                                │
     │   - EurekaInstanceAction - журнал действий                                                                                                                                        │
     │ 2. Расширение модели Server:                                                                                                                                                      │
     │   - Добавить поле is_eureka_node (Узел Eureka)                                                                                                                                    │
     │   - Создать миграцию БД                                                                                                                                                           │
     │ 3. Конфигурация (app/config.py):                                                                                                                                                  │
     │   - Параметры подключения и интервалы опроса                                                                                                                                      │
     │   - Таймауты и настройки кэша                                                                                                                                                     │
     │                                                                                                                                                                                   │
     │ Фаза 2: Сервисный слой                                                                                                                                                            │
     │                                                                                                                                                                                   │
     │ 4. Сервис Eureka (app/services/eureka_service.py):                                                                                                                                │
     │   - Взаимодействие с API агента для получения данных Eureka                                                                                                                       │
     │   - Методы согласно EUREKA_CONTROL_API.md:                                                                                                                                        │
     │       - get_all_applications()                                                                                                                                                    │
     │     - health_check()                                                                                                                                                              │
     │     - pause_application()                                                                                                                                                         │
     │     - shutdown_application()                                                                                                                                                      │
     │     - set_log_level()                                                                                                                                                             │
     │   - Кэширование и обработка ошибок                                                                                                                                                │
     │ 5. Маппер Eureka (app/services/eureka_mapper.py):                                                                                                                                 │
     │   - УПРОЩЁННАЯ ЛОГИКА МАППИНГА:                                                                                                                                                   │
     │       - Для docker-приложений: прямое сопоставление по Application.eureka_url с IP:port из Eureka instance_id                                                                     │
     │     - Пример: если app.eureka_url = "192.168.1.100:8080" и Eureka instance_id = "192.168.1.100:user-service:8080", то это совпадение                                              │
     │   - Резервные стратегии для non-docker приложений                                                                                                                                 │
     │   - Поддержка ручного маппинга                                                                                                                                                    │
     │                                                                                                                                                                                   │
     │ Фаза 3: API                                                                                                                                                                       │
     │                                                                                                                                                                                   │
     │ 6. API эндпоинты (app/api/eureka_routes.py):                                                                                                                                      │
     │   - CRUD для серверов Eureka                                                                                                                                                      │
     │   - Мониторинг приложений и экземпляров                                                                                                                                           │
     │   - Операции управления (pause, shutdown, log level)                                                                                                                              │
     │   - Эндпоинт для получения маппинга между AC приложениями и Eureka экземплярами                                                                                                   │
     │                                                                                                                                                                                   │
     │ Фаза 4: Мониторинг                                                                                                                                                                │
     │                                                                                                                                                                                   │
     │ 7. Интеграция в мониторинг (app/tasks/monitoring.py):                                                                                                                             │
     │   - Периодическая синхронизация с Eureka через агентов                                                                                                                            │
     │   - Автоматический маппинг по eureka_url                                                                                                                                          │
     │   - Обновление статусов                                                                                                                                                           │
     │   - Очистка старой истории                                                                                                                                                        │
     │                                                                                                                                                                                   │
     │ Фаза 5: Интерфейс                                                                                                                                                                 │
     │                                                                                                                                                                                   │
     │ 8. Дашборд Eureka:                                                                                                                                                                │
     │   - UI из eureka_dashboard_final-1.html                                                                                                                                           │
     │   - Отображение связи между AC приложениями и Eureka экземплярами                                                                                                                 │
     │   - Операции: health check, pause, shutdown, log level                                                                                                                            │
     │   - Статистика: Total, UP, DOWN, PAUSED                                                                                                                                           │
     │ 9. Страница сервера:                                                                                                                                                              │
     │   - Чекбокс "Узел Eureka"                                                                                                                                                         │
     │   - Показ Eureka приложений на этом узле                                                                                                                                          │
     │ 10. Страница приложения:                                                                                                                                                          │
     │ - Если есть eureka_url, показывать статус в Eureka                                                                                                                                │
     │ - Быстрые действия Eureka прямо со страницы приложения                                                                                                                            │
     │                                                                                                                                                                                   │
     │ Фаза 6: Миграции БД                                                                                                                                                               │
     │                                                                                                                                                                                   │
     │ 11. Создание миграций:                                                                                                                                                            │
     │   - Новые таблицы для Eureka моделей                                                                                                                                              │
     │   - Добавление is_eureka_node в servers                                                                                                                                           │
     │   - Связующие таблицы для маппинга                                                                                                                                                │
     │                                                                                                                                                                                   │
     │ Ключевое преимущество:                                                                                                                                                            │
     │                                                                                                                                                                                   │
     │ Благодаря существующему полю eureka_url в модели Application, маппинг становится простым и надёжным - прямое сопоставление IP:port из eureka_url с IP:port из Eureka instance_id. │
     ╰───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

> Опиши план детальнее под каждым пунктом, а так же опиши какие таблицы в БД будут созданы

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 Ready to code?

 Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 Детальный план реализации интеграции сервиса Eureka

 Фаза 1: Модели БД и базовая инфраструктура

 1. Создание моделей для Eureka (app/models/eureka.py)

 1.1. Таблица eureka_servers
 class EurekaServer(db.Model):
     id = Integer, primary_key
     server_id = Integer, ForeignKey('servers.id')  # Связь с таблицей servers
     eureka_host = String(255)  # Хост Eureka реестра
     eureka_port = Integer  # Порт Eureka реестра
     is_active = Boolean, default=True  # Активен ли сервер
     last_sync = DateTime  # Последняя успешная синхронизация
     last_error = Text  # Последняя ошибка
     created_at = DateTime
     updated_at = DateTime
     removed_at = DateTime  # Для мягкого удаления

 1.2. Таблица eureka_applications
 class EurekaApplication(db.Model):
     id = Integer, primary_key
     eureka_server_id = Integer, ForeignKey('eureka_servers.id')
     app_name = String(255)  # Имя приложения в Eureka (SERVICE-NAME)
     instances_count = Integer  # Количество экземпляров
     instances_up = Integer  # Количество работающих
     instances_down = Integer  # Количество упавших
     instances_paused = Integer  # Количество на паузе
     last_sync = DateTime
     created_at = DateTime
     updated_at = DateTime

 1.3. Таблица eureka_instances
 class EurekaInstance(db.Model):
     id = Integer, primary_key
     eureka_application_id = Integer, ForeignKey('eureka_applications.id')
     instance_id = String(255)  # Формат: IP:service-name:port
     ip_address = String(45)  # IP адрес экземпляра
     port = Integer  # Порт экземпляра
     service_name = String(255)  # Имя сервиса
     status = String(50)  # UP, DOWN, PAUSED, STARTING, OUT_OF_SERVICE
     last_heartbeat = DateTime  # Последний heartbeat
     metadata = JSON  # Метаданные (version, environment и т.д.)
     health_check_url = String(512)  # URL для health check
     home_page_url = String(512)  # URL домашней страницы
     status_page_url = String(512)  # URL страницы статуса
     application_id = Integer, ForeignKey('applications.id')  # Связь с AC приложением
     is_manual_mapping = Boolean, default=False  # Ручной маппинг
     created_at = DateTime
     updated_at = DateTime
     removed_at = DateTime  # Для мягкого удаления

 1.4. Таблица eureka_instance_status_history
 class EurekaInstanceStatusHistory(db.Model):
     id = Integer, primary_key
     eureka_instance_id = Integer, ForeignKey('eureka_instances.id')
     old_status = String(50)
     new_status = String(50)
     reason = Text  # Причина изменения статуса
     changed_at = DateTime
     changed_by = String(255)  # Кто/что изменило (system, user, health_check)

 1.5. Таблица eureka_instance_actions
 class EurekaInstanceAction(db.Model):
     id = Integer, primary_key
     eureka_instance_id = Integer, ForeignKey('eureka_instances.id')
     action_type = String(50)  # health_check, pause, shutdown, log_level_change
     action_params = JSON  # Параметры действия
     status = String(50)  # pending, in_progress, success, failed
     result = Text  # Результат выполнения
     error_message = Text  # Сообщение об ошибке
     user_id = Integer, ForeignKey('users.id')  # Кто инициировал
     started_at = DateTime
     completed_at = DateTime

 2. Расширение модели Server (app/models/server.py)

 Добавление поля:
 is_eureka_node = db.Column(db.Boolean, default=False, nullable=False)
 # Индикатор того, что на этом сервере установлен Eureka реестр

 Добавление связей:
 eureka_server = db.relationship('EurekaServer', back_populates='server', uselist=False)

 3. Конфигурация (app/config.py)

 Добавление переменных:
 # Основные настройки
 EUREKA_ENABLED = os.environ.get('EUREKA_ENABLED', 'true').lower() == 'true'

 # Параметры подключения
 EUREKA_REQUEST_TIMEOUT = int(os.environ.get('EUREKA_REQUEST_TIMEOUT', '10'))  # секунды
 EUREKA_MAX_RETRIES = int(os.environ.get('EUREKA_MAX_RETRIES', '3'))
 EUREKA_RETRY_DELAY = int(os.environ.get('EUREKA_RETRY_DELAY', '1'))  # секунды

 # Интервалы синхронизации
 EUREKA_POLLING_INTERVAL = int(os.environ.get('EUREKA_POLLING_INTERVAL', '60'))  # секунды
 EUREKA_HEALTH_CHECK_INTERVAL = int(os.environ.get('EUREKA_HEALTH_CHECK_INTERVAL', '30'))

 # Кэширование
 EUREKA_CACHE_TTL = int(os.environ.get('EUREKA_CACHE_TTL', '30'))  # секунды
 EUREKA_CACHE_MAX_SIZE = int(os.environ.get('EUREKA_CACHE_MAX_SIZE', '1000'))

 # Хранение истории
 EUREKA_HISTORY_RETENTION_DAYS = int(os.environ.get('EUREKA_HISTORY_RETENTION_DAYS', '30'))
 EUREKA_MAX_HISTORY_RECORDS = int(os.environ.get('EUREKA_MAX_HISTORY_RECORDS', '10000'))

 Фаза 2: Сервисный слой

 4. Сервис Eureka (app/services/eureka_service.py)

 Основные методы:

 class EurekaService:
     def __init__(self):
         self.cache = {}  # Кэш с TTL
         self.session = aiohttp.ClientSession()

     async def get_all_applications(self, eureka_server_id: int):
         """
         Получить все приложения из Eureka через агента
         1. Найти EurekaServer по ID
         2. Получить связанный Server
         3. Вызвать FAgent API: GET /api/v1/eureka/apps
         4. Парсинг ответа и создание/обновление EurekaApplication и EurekaInstance
         5. Кэширование результата
         """

     async def get_application_details(self, instance_id: str):
         """
         Получить детали конкретного приложения
         1. Найти EurekaInstance по instance_id
         2. Проверить кэш
         3. Вызвать FAgent API: GET /api/v1/eureka/apps/{instance_id}
         4. Обновить данные в БД
         """

     async def health_check(self, instance_id: str):
         """
         Проверка здоровья экземпляра
         1. Найти EurekaInstance
         2. Вызвать FAgent API: GET /api/v1/eureka/apps/{instance_id}/health
         3. Обновить статус в БД
         4. Создать запись в eureka_instance_status_history
         5. Вернуть результат
         """

     async def pause_application(self, instance_id: str, user_id: int = None):
         """
         Поставить приложение на паузу
         1. Создать запись в eureka_instance_actions
         2. Вызвать FAgent API: POST /api/v1/eureka/apps/{instance_id}/pause
         3. Обновить статус экземпляра
         4. Записать в историю
         5. Обновить запись действия с результатом
         """

     async def shutdown_application(self, instance_id: str, user_id: int = None):
         """
         Остановить приложение
         1. Создать запись в eureka_instance_actions
         2. Вызвать FAgent API: POST /api/v1/eureka/apps/{instance_id}/shutdown
         3. Обновить статус на DOWN
         4. Записать в историю
         5. Обновить запись действия
         """

     async def set_log_level(self, instance_id: str, logger: str, level: str, user_id: int = None):
         """
         Изменить уровень логирования
         1. Валидация уровня (DEBUG, INFO, WARN, ERROR)
         2. Создать запись в eureka_instance_actions
         3. Вызвать FAgent API: POST /api/v1/eureka/apps/{instance_id}/loglevel
            Body: {"logger": logger, "level": level}
         4. Обновить запись действия с результатом
         """

     async def sync_eureka_applications(self):
         """
         Полная синхронизация всех Eureka серверов
         1. Получить все активные EurekaServer где is_active=True
         2. Для каждого сервера вызвать get_all_applications()
         3. Обновить статистику в eureka_applications
         4. Вызвать маппер для связывания с AC приложениями
         5. Пометить removed_at для исчезнувших экземпляров
         6. Логирование ошибок
         """

     def _handle_agent_error(self, error):
         """Обработка ошибок от агента с retry логикой"""

     def _update_cache(self, key, value):
         """Обновление кэша с TTL"""

 5. Маппер Eureka (app/services/eureka_mapper.py)

 Логика маппинга:

 class EurekaMapper:
     def map_instances_to_applications(self):
         """
         Основной метод маппинга
         1. Получить все EurekaInstance где application_id IS NULL и is_manual_mapping=False
         2. Для каждого экземпляра:
            a. Извлечь IP:port из instance_id (первая и последняя часть)
            b. Найти Application где eureka_url совпадает с IP:port
            c. Если найдено - установить application_id
            d. Если не найдено - попробовать альтернативные стратегии
         3. Логирование результатов маппинга
         """

     def map_by_eureka_url(self, instance: EurekaInstance):
         """
         Маппинг по eureka_url (для Docker приложений)
         1. Извлечь IP:port из instance_id (формат: IP:service-name:port)
         2. Найти Application где eureka_url = IP:port
         3. Вернуть application_id или None
         """

     def map_by_server_and_name(self, instance: EurekaInstance):
         """
         Маппинг по серверу и имени (резервная стратегия)
         1. Найти Server по IP адресу из instance
         2. Извлечь имя сервиса из instance_id
         3. Найти Application на этом сервере с похожим именем
         4. Использовать fuzzy matching для имён
         """

     def set_manual_mapping(self, instance_id: int, application_id: int):
         """
         Установить ручной маппинг
         1. Найти EurekaInstance
         2. Установить application_id и is_manual_mapping=True
         3. Создать запись в истории маппинга
         """

     def clear_manual_mapping(self, instance_id: int):
         """
         Очистить ручной маппинг
         1. Установить application_id=NULL, is_manual_mapping=False
         2. Запустить автоматический маппинг для этого экземпляра
         """

 Фаза 3: API

 6. API эндпоинты (app/api/eureka_routes.py)

 Структура эндпоинтов:

 # Серверы Eureka
 GET  /api/eureka/servers
      - Список всех Eureka серверов
      - Фильтры: is_active, server_id
      - Включает статистику по приложениям

 POST /api/eureka/servers
      - Создание нового Eureka сервера
      - Body: {server_id, eureka_host, eureka_port}
      - Валидация уникальности

 GET  /api/eureka/servers/<id>
      - Детали Eureka сервера
      - Включает список приложений

 PUT  /api/eureka/servers/<id>
      - Обновление параметров
      - Можно изменить host, port, is_active

 DELETE /api/eureka/servers/<id>
      - Мягкое удаление (removed_at)

 # Приложения и экземпляры
 GET  /api/eureka/applications
      - Список всех приложений из всех Eureka
      - Фильтры: eureka_server_id, app_name, status
      - Пагинация

 GET  /api/eureka/instances
      - Список всех экземпляров
      - Фильтры: status, application_id, ip_address
      - Включает информацию о маппинге

 GET  /api/eureka/instances/<instance_id>
      - Детали экземпляра
      - История статусов
      - История действий

 # Операции
 POST /api/eureka/instances/<instance_id>/health
      - Запуск health check
      - Возвращает результат проверки

 POST /api/eureka/instances/<instance_id>/pause
      - Поставить на паузу
      - Body: {reason}

 POST /api/eureka/instances/<instance_id>/shutdown
      - Остановить экземпляр
      - Body: {graceful: true/false}

 POST /api/eureka/instances/<instance_id>/loglevel
      - Изменить уровень логирования
      - Body: {logger, level}

 # Маппинг
 GET  /api/eureka/instances/unmapped
      - Список несвязанных экземпляров

 POST /api/eureka/instances/<id>/map
      - Установить ручной маппинг
      - Body: {application_id}

 DELETE /api/eureka/instances/<id>/map
      - Удалить ручной маппинг

 # Синхронизация
 POST /api/eureka/sync
      - Принудительная синхронизация всех серверов

 POST /api/eureka/servers/<id>/sync
      - Синхронизация конкретного сервера

 # Статистика
 GET  /api/eureka/summary
      - Общая статистика
      - Количество серверов, приложений, экземпляров
      - Распределение по статусам

 GET  /api/eureka/history
      - История изменений статусов
      - Фильтры: date_from, date_to, instance_id

 Фаза 4: Мониторинг

 7. Интеграция в мониторинг (app/tasks/monitoring.py)

 Добавление задач:

 async def monitor_eureka_servers():
     """
     Задача мониторинга Eureka серверов
     Запускается каждые EUREKA_POLLING_INTERVAL секунд

     1. Получить все активные EurekaServer
     2. Для каждого сервера:
        a. Вызвать EurekaService.sync_eureka_applications()
        b. Обновить last_sync или last_error
        c. Проверить изменения статусов
        d. Создать события для критических изменений
     3. Запустить маппер для новых экземпляров
     4. Очистить старую историю (старше EUREKA_HISTORY_RETENTION_DAYS)
     """

 async def health_check_eureka_instances():
     """
     Периодическая проверка здоровья
     Запускается каждые EUREKA_HEALTH_CHECK_INTERVAL секунд

     1. Получить экземпляры для проверки (status != DOWN)
     2. Batch health checks через asyncio
     3. Обновить статусы
     4. Создать алерты для упавших сервисов
     """

 def cleanup_eureka_history():
     """
     Очистка старых записей
     1. Удалить из eureka_instance_status_history старше X дней
     2. Удалить из eureka_instance_actions старше X дней
     3. Удалить экземпляры с removed_at старше X дней
     """

 Фаза 5: Интерфейс

 8. Дашборд Eureka

 Компоненты UI (на основе eureka_dashboard_final-1.html):

 // Основные компоненты

 1. Статистические карточки:
    - Total Services (общее количество)
    - UP (зелёная карточка)
    - DOWN (красная карточка)
    - PAUSED (жёлтая карточка)

 2. Фильтры и поиск:
    - Выпадающий список статусов
    - Поиск по имени сервиса
    - Выбор Eureka сервера

 3. Таблица экземпляров:
    - Instance ID
    - Service Name
    - Status (с цветовой индикацией)
    - IP:Port
    - AC Application (если связано)
    - Actions (кнопки действий)

 4. Кнопки действий:
    - Health Check (✓)
    - Log Level (📝) - открывает модальное окно
    - Pause (⏸)
    - Shutdown (✕)

 5. Модальное окно Log Level:
    - Выбор logger (выпадающий список)
    - Выбор уровня (DEBUG, INFO, WARN, ERROR)
    - Кнопки Apply/Cancel

 6. Auto-refresh:
    - Выпадающий список (30s, 1m, 5m, off)
    - SSE подключение для real-time обновлений

 7. Toast уведомления:
    - Успешные операции (зелёные)
    - Ошибки (красные)
    - Информационные (синие)

 9. Страница сервера

 Добавления в существующую страницу:

 <!-- В форме редактирования сервера -->
 <div class="form-check">
     <input type="checkbox" id="is_eureka_node" name="is_eureka_node">
     <label for="is_eureka_node">Узел Eureka</label>
 </div>

 <!-- Если is_eureka_node=true, показать дополнительную секцию -->
 <div id="eureka_config" style="display:none">
     <h3>Конфигурация Eureka</h3>
     <input type="text" name="eureka_host" placeholder="Eureka Host">
     <input type="number" name="eureka_port" placeholder="Eureka Port">

     <!-- Статистика -->
     <div class="eureka-stats">
         <p>Приложений: <span id="apps_count">0</span></p>
         <p>Экземпляров: <span id="instances_count">0</span></p>
         <p>Последняя синхронизация: <span id="last_sync">-</span></p>
     </div>

     <!-- Список приложений на этом Eureka -->
     <table id="eureka_apps_table">
         <!-- Динамически загружаемый контент -->
     </table>
 </div>

 10. Страница приложения

 Добавления для приложений с eureka_url:

 <!-- Если у приложения есть eureka_url -->
 <div class="eureka-section">
     <h3>Eureka статус</h3>
     <p>Eureka URL: {{ app.eureka_url }}</p>
     <p>Статус в Eureka: <span class="status-badge">{{ eureka_status }}</span></p>
     <p>Instance ID: {{ eureka_instance_id }}</p>

     <!-- Быстрые действия -->
     <div class="eureka-actions">
         <button onclick="healthCheck()">Health Check</button>
         <button onclick="pauseInEureka()">Pause</button>
         <button onclick="openLogLevelModal()">Set Log Level</button>
     </div>

     <!-- История последних действий -->
     <div class="eureka-history">
         <h4>Последние операции Eureka</h4>
         <ul id="eureka_actions_list">
             <!-- Динамически загружаемый список -->
         </ul>
     </div>
 </div>

 Фаза 6: Миграции БД

 11. Создание миграций

 Команда для генерации:
 flask db migrate -m "Add Eureka integration models"

 Содержимое миграции будет включать:

 -- Создание таблиц
 CREATE TABLE eureka_servers (...);
 CREATE TABLE eureka_applications (...);
 CREATE TABLE eureka_instances (...);
 CREATE TABLE eureka_instance_status_history (...);
 CREATE TABLE eureka_instance_actions (...);

 -- Индексы для производительности
 CREATE INDEX idx_eureka_instances_status ON eureka_instances(status);
 CREATE INDEX idx_eureka_instances_instance_id ON eureka_instances(instance_id);
 CREATE INDEX idx_eureka_instances_application_id ON eureka_instances(application_id);
 CREATE INDEX idx_eureka_instance_status_history_instance ON eureka_instance_status_history(eureka_instance_id);
 CREATE INDEX idx_eureka_instance_actions_instance ON eureka_instance_actions(eureka_instance_id);

 -- Изменение существующих таблиц
 ALTER TABLE servers ADD COLUMN is_eureka_node BOOLEAN DEFAULT FALSE;

 -- Foreign keys
 ALTER TABLE eureka_servers ADD CONSTRAINT fk_eureka_server_server
   FOREIGN KEY (server_id) REFERENCES servers(id);
 ALTER TABLE eureka_instances ADD CONSTRAINT fk_eureka_instance_application
   FOREIGN KEY (application_id) REFERENCES applications(id);

 Итоговая структура БД:

 Новые таблицы:
 1. eureka_servers - Eureka реестры
 2. eureka_applications - Приложения в Eureka
 3. eureka_instances - Экземпляры сервисов
 4. eureka_instance_status_history - История статусов
 5. eureka_instance_actions - Журнал операций

 Изменённые таблицы:
 1. servers - добавлено поле is_eureka_node

 Ключевые связи:
 - eureka_servers → servers (один к одному)
 - eureka_applications → eureka_servers (многие к одному)
 - eureka_instances → eureka_applications (многие к одному)
 - eureka_instances → applications (многие к одному, для маппинга)
 - История и действия → eureka_instances (многие к одному)
