# Механизм проверки доступности данных от агентов

> **Дата:** 2025-12-01
> **Статус:** Реализовано

## Цель

Отслеживание и отображение состояния получения данных от FAgent для HAProxy и Eureka сервисов. При недоступности агента или ошибке получения данных — информировать пользователя через визуальные индикаторы и уведомления.

---

## Общая архитектура

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              POLLING CYCLE                                   │
│                     (Периодический опрос агентов)                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           BACKEND SERVICES                                   │
│  ┌─────────────────────────────┐   ┌─────────────────────────────────────┐  │
│  │   haproxy_service.py        │   │   eureka_service.py                 │  │
│  │                             │   │                                     │  │
│  │   sync_haproxy_data()       │   │   sync_eureka_data()                │  │
│  │         │                   │   │         │                           │  │
│  │         ▼                   │   │         ▼                           │  │
│  │   FAgent API Request        │   │   FAgent API Request                │  │
│  │         │                   │   │         │                           │  │
│  │    ┌────┴────┐              │   │    ┌────┴────┐                      │  │
│  │    │         │              │   │    │         │                      │  │
│  │ SUCCESS   FAILED            │   │ SUCCESS   FAILED                    │  │
│  │    │         │              │   │    │         │                      │  │
│  │    ▼         ▼              │   │    ▼         ▼                      │  │
│  │ mark_fetch  mark_fetch      │   │ mark_fetch  mark_fetch              │  │
│  │ _success()  _failed(err)    │   │ _success()  _failed(err)            │  │
│  └─────────────────────────────┘   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              DATABASE                                        │
│                                                                             │
│  HAProxyBackend                        EurekaApplication                    │
│  ┌─────────────────────────┐          ┌─────────────────────────┐          │
│  │ last_fetch_status       │          │ last_fetch_status       │          │
│  │   'success' | 'failed'  │          │   'success' | 'failed'  │          │
│  │                         │          │                         │          │
│  │ last_fetch_error        │          │ last_fetch_error        │          │
│  │   NULL | "Error text"   │          │   NULL | "Error text"   │          │
│  │                         │          │                         │          │
│  │ last_fetch_at           │          │ last_fetch_at           │          │
│  │   timestamp             │          │   timestamp             │          │
│  └─────────────────────────┘          └─────────────────────────┘          │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API RESPONSE                                    │
│                                                                             │
│  GET /api/haproxy/backends          GET /api/eureka/instances               │
│  {                                  {                                       │
│    "backends": [{                     "instances": [{                       │
│      "last_fetch_status": "failed",     "eureka_application": {             │
│      "last_fetch_error": "...",           "last_fetch_status": "failed",   │
│      "last_fetch_at": "..."               "last_fetch_error": "..."        │
│    }]                                   }                                   │
│  }                                    }]                                    │
│                                     }                                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND                                        │
│  ┌─────────────────────────────┐   ┌─────────────────────────────────────┐  │
│  │   haproxy/ui.js             │   │   eureka/ui.js                      │  │
│  │                             │   │                                     │  │
│  │   if (last_fetch_status     │   │   if (last_fetch_status             │  │
│  │       === 'failed')         │   │       === 'failed')                 │  │
│  │   {                         │   │   {                                 │  │
│  │     - Show ⚠️ icon          │   │     - Show ⚠️ icon                  │  │
│  │     - Show error block      │   │     - Show error block              │  │
│  │     - Add 'has-error' class │   │     - Add error indicator           │  │
│  │   }                         │   │   }                                 │  │
│  └─────────────────────────────┘   └─────────────────────────────────────┘  │
│                                                                             │
│  ┌─────────────────────────────┐   ┌─────────────────────────────────────┐  │
│  │   haproxy/manager.js        │   │   eureka/manager.js                 │  │
│  │                             │   │                                     │  │
│  │   Track errors in Set       │   │   Track errors in Set               │  │
│  │   On new error:             │   │   On new error:                     │  │
│  │     showNotification()      │   │     showNotification()              │  │
│  └─────────────────────────────┘   └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Модели данных

### HAProxyBackend (`app/models/haproxy.py`)

```python
class HAProxyBackend(db.Model):
    # ... другие поля ...

    last_fetch_status = db.Column(db.String(20), default='unknown')
    last_fetch_error = db.Column(db.Text, nullable=True)
    last_fetch_at = db.Column(db.DateTime, nullable=True)

    def mark_fetch_success(self):
        """Отметить успешное получение данных от агента"""
        self.last_fetch_status = 'success'
        self.last_fetch_error = None
        self.last_fetch_at = datetime.utcnow()

    def mark_fetch_failed(self, error_message):
        """Отметить неудачную попытку получения данных от агента"""
        self.last_fetch_status = 'failed'
        self.last_fetch_error = error_message
        self.last_fetch_at = datetime.utcnow()
```

### EurekaApplication (`app/models/eureka.py`)

```python
class EurekaApplication(db.Model):
    # ... другие поля ...

    last_fetch_status = db.Column(db.String(20), default='unknown')
    last_fetch_error = db.Column(db.Text, nullable=True)
    last_fetch_at = db.Column(db.DateTime, nullable=True)

    def mark_fetch_success(self):
        self.last_fetch_status = 'success'
        self.last_fetch_error = None
        self.last_fetch_at = datetime.utcnow()

    def mark_fetch_failed(self, error_message):
        self.last_fetch_status = 'failed'
        self.last_fetch_error = error_message
        self.last_fetch_at = datetime.utcnow()
```

---

## Логика обработки ошибок

### HAProxy Service (`app/services/haproxy_service.py`)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    sync_haproxy_backends_data()                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │  Для каждого backend с         │
                    │  enable_polling = True         │
                    └────────────────────────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │  Запрос к FAgent:              │
                    │  GET /haproxy/stats/{backend}  │
                    └────────────────────────────────┘
                                     │
                         ┌───────────┴───────────┐
                         │                       │
                    SUCCESS                   FAILED
                         │                       │
                         ▼                       ▼
            ┌────────────────────┐   ┌────────────────────────┐
            │ Обработка серверов │   │ backend.mark_fetch_    │
            │ в backend          │   │ failed(error_msg)      │
            │                    │   │                        │
            │ backend.mark_fetch │   │ continue → next backend│
            │ _success()         │   └────────────────────────┘
            └────────────────────┘
```

**Код (`haproxy_service.py:408-415`):**
```python
if not success:
    error_msg = f"Не удалось получить серверы для backend {backend_name}"
    logger.warning(error_msg)
    backend.mark_fetch_failed(error_msg)
    db.session.commit()
    continue

# ... обработка серверов ...
backend.mark_fetch_success()
```

### Eureka Service (`app/services/eureka_service.py`)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    sync_eureka_applications()                                │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │  Запрос к Eureka Server:       │
                    │  GET /eureka/apps              │
                    └────────────────────────────────┘
                                     │
                         ┌───────────┴───────────┐
                         │                       │
                    SUCCESS                   FAILED
                         │                       │
                         ▼                       ▼
            ┌────────────────────┐   ┌────────────────────────────┐
            │ Для каждого app:   │   │ ВСЕ приложения сервера:    │
            │                    │   │                            │
            │ try:               │   │ for app in existing_apps:  │
            │   process_app()    │   │   app.mark_fetch_failed()  │
            │   app.mark_fetch   │   │                            │
            │   _success()       │   │ return False               │
            │ except:            │   └────────────────────────────┘
            │   app.mark_fetch   │
            │   _failed(err)     │
            └────────────────────┘
```

**Особенность Eureka:** При ошибке подключения к серверу — отмечаются ВСЕ приложения этого сервера.

**Код (`eureka_service.py:546-554`):**
```python
except Exception as e:
    error_message = f"Ошибка подключения к Eureka {eureka_server.url}: {str(e)}"
    logger.error(error_message)

    # Отмечаем все приложения этого сервера как failed
    existing_apps = EurekaApplication.query.filter_by(
        eureka_server_id=eureka_server.id
    ).all()
    for app in existing_apps:
        app.mark_fetch_failed(error_message)
```

---

## Frontend отображение

### HAProxy UI (`app/static/js/haproxy/ui.js:60-91`)

```javascript
// Проверка на ошибку
const hasError = backend.last_fetch_status === 'failed';

// Индикатор ошибки
const errorIndicator = hasError
    ? '<span class="backend-error-indicator" title="' +
      (backend.last_fetch_error || 'Ошибка получения данных от агента') +
      '">⚠️</span>'
    : '';

// CSS класс для подсветки
const errorClass = hasError ? 'has-error' : '';

// Блок с подробностями ошибки
if (hasError) {
    lastFetchInfo = `
        <div class="backend-error-message">
            <strong>Ошибка:</strong> ${backend.last_fetch_error}
            <div class="backend-error-time">
                Последняя попытка: ${timeAgo}
            </div>
        </div>
    `;
}
```

### Eureka UI (`app/static/js/eureka/ui.js:112-134`)

```javascript
// Проверка на ошибку приложения
const hasFetchError = instance.eureka_application?.last_fetch_status === 'failed';

// Сообщение об ошибке
const errorMessage = hasFetchError && instance.eureka_application.last_fetch_error
    ? `
        <div class="fetch-error-message">
            <i class="fas fa-exclamation-triangle"></i>
            <div class="fetch-error-text">
                <strong>Ошибка получения данных от агента:</strong><br>
                ${this.escapeHtml(instance.eureka_application.last_fetch_error)}
            </div>
        </div>
    `
    : '';

// Индикатор в строке таблицы
${hasFetchError
    ? '<span class="error-indicator"><i class="fas fa-exclamation-circle"></i></span>'
    : ''}
```

---

## Система уведомлений

### HAProxy Manager (`app/static/js/haproxy/manager.js:226-252`)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    checkForNewErrors(backends)                               │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│   1. Найти все backends с last_fetch_status === 'failed'                    │
│   2. Сравнить с previousErrorBackends (Set)                                 │
│   3. Если есть НОВЫЕ ошибки (которых не было раньше):                       │
│      - showNotification("⚠️ Ошибка получения данных...", 'warning')         │
│   4. Обновить previousErrorBackends                                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

```javascript
// Отслеживание ошибок между обновлениями
this.previousErrorBackends = new Set();

checkForNewErrors(backends) {
    // Находим бэкенды с ошибками
    const errorBackends = backends.filter(b => b.last_fetch_status === 'failed');
    const newErrors = [];

    for (const backend of errorBackends) {
        // Если это новая ошибка (не была в предыдущей проверке)
        if (!this.previousErrorBackends.has(backend.id)) {
            newErrors.push({
                id: backend.id,
                name: backend.backend_name,
                error: backend.last_fetch_error || 'Неизвестная ошибка'
            });
        }
    }

    // Показываем уведомление о новых ошибках
    if (newErrors.length > 0) {
        const message = newErrors.length === 1
            ? `⚠️ Ошибка получения данных для бэкенда "${newErrors[0].name}"`
            : `⚠️ Обнаружено ${newErrors.length} бэкендов с ошибками`;

        window.showNotification(message, 'warning');
    }

    // Обновляем Set для следующей проверки
    this.previousErrorBackends = new Set(errorBackends.map(b => b.id));
}
```

---

## Визуальное отображение

### HAProxy — Backend с ошибкой

```
┌─────────────────────────────────────────────────────────────────┐
│  ▼ backend-jurws ⚠️                           [UP: 0] [DOWN: 3] │
├─────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  ⚠️ Ошибка: Connection refused to agent fdmz01:8080       │ │
│  │     Последняя попытка: 5 мин. назад                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Серверы:                                                       │
│  ┌─────────────┬────────────┬─────────┐                        │
│  │ Сервер      │ Статус     │ Вес     │                        │
│  ├─────────────┼────────────┼─────────┤                        │
│  │ jurws_1     │ ⚫ no data │ 100     │                        │
│  │ jurws_2     │ ⚫ no data │ 100     │                        │
│  └─────────────┴────────────┴─────────┘                        │
└─────────────────────────────────────────────────────────────────┘
```

### Eureka — Instance с ошибкой приложения

```
┌────────────────────────────────────────────────────────────────────────────┐
│ Instance ID        │ Service          │ IP           │ Port │ Status      │
├────────────────────┼──────────────────┼──────────────┼──────┼─────────────┤
│ fdmz01:jurws:8080  │ JURWS ⚠️         │ 10.0.0.1     │ 8080 │ 🟢 UP       │
│                    │ ┌──────────────────────────────────────────────────┐ │
│                    │ │ ⚠️ Ошибка получения данных от агента:           │ │
│                    │ │    Connection timeout to eureka server          │ │
│                    │ └──────────────────────────────────────────────────┘ │
├────────────────────┼──────────────────┼──────────────┼──────┼─────────────┤
│ fdmz02:jurws:8080  │ JURWS            │ 10.0.0.2     │ 8080 │ 🟢 UP       │
└────────────────────┴──────────────────┴──────────────┴──────┴─────────────┘
```

---

## Сравнительная таблица

| Компонент | HAProxy | Eureka |
|-----------|---------|--------|
| **Модель с состоянием** | `HAProxyBackend` | `EurekaApplication` |
| **Поле статуса** | `last_fetch_status` | `last_fetch_status` |
| **Поле ошибки** | `last_fetch_error` | `last_fetch_error` |
| **Поле времени** | `last_fetch_at` | `last_fetch_at` |
| **Сервис синхронизации** | `haproxy_service.py` | `eureka_service.py` |
| **UI компонент** | `haproxy/ui.js` | `eureka/ui.js` |
| **Manager уведомлений** | `haproxy/manager.js` | `eureka/manager.js` |
| **Гранулярность ошибки** | Per backend | Per application |
| **Иконка ошибки** | ⚠️ emoji | FontAwesome icon |
| **CSS класс** | `.has-error` | `.error-indicator` |

---

## Изменённые файлы

| Файл | Назначение |
|------|------------|
| `app/models/haproxy.py` | Модель HAProxyBackend с полями статуса |
| `app/models/eureka.py` | Модель EurekaApplication с полями статуса |
| `app/services/haproxy_service.py` | Логика mark_fetch_success/failed |
| `app/services/eureka_service.py` | Логика mark_fetch_success/failed |
| `app/static/js/haproxy/ui.js` | Отображение ошибок в UI |
| `app/static/js/haproxy/manager.js` | Отслеживание новых ошибок |
| `app/static/js/eureka/ui.js` | Отображение ошибок в UI |
| `app/static/js/eureka/manager.js` | Отслеживание новых ошибок |

---

## Возможные улучшения

1. **Retry механизм** — автоматические повторные попытки с exponential backoff
2. **Health endpoint** — отдельный API для проверки состояния всех агентов
3. **Dashboard виджет** — сводка по ошибкам на главной странице
4. **Email/Slack уведомления** — при критических ошибках
5. **История ошибок** — отдельная таблица для анализа паттернов сбоев
