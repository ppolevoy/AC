# Автообновление страницы Applications

## Описание

Страница applications автоматически обновляет данные каждые **20 секунд**.

## Как это работает

1. При загрузке страницы автоматически запускается таймер
2. Каждые 20 секунд происходит автоматическая загрузка данных
3. При ручном нажатии кнопки "⟳" таймер сбрасываетс и начинается заново
4. **УМНАЯ ПАУЗА**: Если открыто модальное окно, обновление откладывается (максимум 10 раз = 3.3 минуты)
5. После 10 пропусков происходит принудительное обновление (защита от зависания)
6. **ЭКОНОМИЯ РЕСУРСОВ**: Не обновляется на неактивных вкладках (Page Visibility API)
7. **ЗАЩИТА ОТ ОШИБОК**: Обработка ошибок и защита от race condition
8. **НАСТРАИВАЕМОСТЬ**: Интервал можно изменить через переменную окружения `AUTO_REFRESH_INTERVAL`

## Управление через консоль браузера

Откройте консоль браузера (F12) и используйте следующие команды:

### Остановить автообновление
```javascript
AutoRefresh.stop()
```

### Запустить автообновление
```javascript
AutoRefresh.start()
```

### Переключить автообновление (вкл/выкл)
```javascript
AutoRefresh.toggle()
```

### Изменить интервал (например, на 30 секунд)
```javascript
AutoRefresh.intervalSeconds = 30
AutoRefresh.restart()
```

### Проверить текущее состояние
```javascript
console.log('Автообновление:', AutoRefresh.isEnabled ? 'включено' : 'выключено')
console.log('Интервал:', AutoRefresh.intervalSeconds, 'секунд')
```

## Примеры использования

**Отключить автообновление на время работы:**
```javascript
AutoRefresh.stop()
// ... делаем что-то на странице ...
AutoRefresh.start()  // включаем обратно
```

**Ускорить обновление для теста:**
```javascript
AutoRefresh.intervalSeconds = 5  // 5 секунд
AutoRefresh.restart()
```

**Вернуть к стандартному интервалу:**
```javascript
AutoRefresh.intervalSeconds = 20
AutoRefresh.restart()
```

## Технические детали

- **Файл**: `app/static/js/applications/applications.js`
- **Модуль**: `AutoRefresh`
- **Интервал по умолчанию**: 20 секунд (настраивается через `AUTO_REFRESH_INTERVAL`)
- **Интеграция**: При ручном обновлении (кнопка ⟳) таймер автоматически сбрасывается
- **Защиты**:
  - Race condition protection (флаг `isLoading`)
  - Обработка ошибок (try-catch)
  - Page Visibility API (не обновляет неактивные вкладки)
  - Cleanup при уходе со страницы
  - Проверка модальных окон через `getComputedStyle()`

## Настройка интервала

### Через переменную окружения (рекомендуется)
```bash
# В .env или docker-compose.yml
AUTO_REFRESH_INTERVAL=30  # секунды
```

### Через консоль браузера
```javascript
AutoRefresh.intervalSeconds = 30
AutoRefresh.restart()
```

## Логирование

Автообновление пишет логи в консоль браузера:
- `🔄 Автообновление запущено (каждые 20с)` - при старте
- `🔄 Автоматическое обновление данных...` - при каждом обновлении
- `⏸️ Автообновление остановлено` - при остановке
- `🔄 Перезапуск таймера автообновления` - при перезапуске
- `👁️ Пропуск обновления: вкладка неактивна` - при неактивной вкладке
- `⏭️ Пропуск обновления: предыдущее обновление еще выполняется` - race condition защита
- `❌ Ошибка при автообновлении: ...` - при ошибке
