# 🐛 Исправление бага управления тегами

## Проблема

При массовом управлении тегами (выбрано несколько приложений), **частичные теги** (теги, которые есть не у всех выбранных приложений) **удалялись** при сохранении, даже если пользователь их не трогал.

### Пример сценария:
1. Выбраны 5 приложений
2. Тег "API" есть у 3 из них (частичный тег, отображается как `(3/5)`)
3. Пользователь открывает модальное окно управления тегами
4. Не трогает чекбокс "API" (он в состоянии indeterminate)
5. Нажимает "Сохранить"
6. **БАГ**: Тег "API" удаляется у всех 3 приложений

## Причина

В функции `showBatchTagsModal()` в файле `applications.js` (строка 2465):

```javascript
// БЫЛО (неправильно):
const checked = isOwned || isInherited ? 'checked' : '';
```

Частичные теги (`isPartial`) **не помечались** как `checked`, только устанавливалось визуальное состояние `indeterminate`.

При сохранении собирались только `checked` теги:
```javascript
const desiredTags = Array.from(
    document.querySelectorAll('.batch-tag-checkbox:checked:not(:disabled)')
).map(cb => cb.value);
```

Результат: частичные теги не попадали в `desiredTags` → бэкенд считал, что их нужно удалить.

## Решение

Изменена логика установки состояния `checked`:

```javascript
// СТАЛО (правильно):
const checked = isOwned || isPartial || isInherited ? 'checked' : '';
```

Теперь частичные теги **отмечаются как checked** по умолчанию и дополнительно получают визуальное состояние `indeterminate`.

### Как работает теперь:

| Состояние тега | Тег у всех | Checked | Indeterminate | Disabled | Поведение |
|----------------|------------|---------|---------------|----------|-----------|
| **У всех** (5/5) | ✅ | ✅ | ❌ | ❌ | Можно снять - удалится у всех |
| **Частичный** (3/5) | ❌ | ✅ | ✅ | ❌ | Можно снять - удалится у всех; оставить - сохранится у 3 |
| **Ни у кого** (0/5) | ❌ | ❌ | ❌ | ❌ | Можно добавить - добавится ко всем |
| **Унаследован** | От группы | ✅ | ❌ | ✅ | Нельзя изменить |

## Преимущества решения

1. ✅ **Сохранение состояния**: Если пользователь не трогает частичный тег, он сохраняется у тех приложений, где был
2. ✅ **Явное удаление**: Чтобы удалить частичный тег у всех, нужно явно снять галочку
3. ✅ **Визуальная индикация**: Состояние `indeterminate` показывает, что тег есть не у всех
4. ✅ **Интуитивность**: Поведение соответствует стандартным UI паттернам (как в файловых менеджерах)

## Измененные файлы

1. **app/static/js/applications/applications.js** (строка 2465-2466)
   - Добавлен `isPartial` в условие для `checked`
   - Добавлен комментарий с объяснением

2. **tag_modal_adapted.html** (демо)
   - Обновлен пример частичного тега (теперь checked)

## Тестирование

### Сценарий 1: Частичный тег НЕ трогаем
- Выбрать 5 приложений
- Тег "Test" есть у 3 из них
- Открыть модальное окно → тег "Test" должен быть checked с indeterminate
- Нажать "Сохранить" БЕЗ изменений
- ✅ **Ожидание**: Тег остается у 3 приложений

### Сценарий 2: Частичный тег СНИМАЕМ
- Выбрать 5 приложений
- Тег "Test" есть у 3 из них
- Открыть модальное окно
- СНЯТЬ галочку с "Test"
- Нажать "Сохранить"
- ✅ **Ожидание**: Тег удаляется у всех 3 приложений

### Сценарий 3: Частичный тег ОСТАВЛЯЕМ checked
- Выбрать 5 приложений
- Тег "Test" есть у 3 из них
- Открыть модальное окно
- Кликнуть на чекбокс "Test" (переключится в полностью checked)
- Нажать "Сохранить"
- ✅ **Ожидание**: Тег добавляется к оставшимся 2 приложениям (теперь у всех 5)

## Дата исправления
2025-11-25
